import { ThemeColors } from 'src/constants/styles/theme/types';
import { colors } from 'src/constants/styles/colors';

const darkColors: ThemeColors = {
  background: colors.neutral[900],
  surface: colors.neutral[800],
  button: colors.primary[500],
  border: colors.neutral[700],
  borderError: colors.error[100],
  textPrimary: colors.neutral[50],
  textSecondary: colors.neutral[200],
  textTertiary: colors.neutral[300],
  textBrand: colors.primary[500],
  textInvert: colors.neutral[900],
};

export { darkColors };
